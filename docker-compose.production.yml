version: '3'

volumes:
  pg_data_production:
  static_volume:
  media_volume:

services:
  db:
    image: postgres:13.10
    env_file: .env
    volumes:
      - pg_data_production:/var/lib/postgresql/data
  backend:
    image: snake85/kitty_backend
    env_file: .env
    volumes:
      - static_volume:/backend_static
      - media_volume:/kitty/media
  frontend:
    image: snake85/kitty_frontend
    env_file: .env
    command: cp -r /app/build/. /frontend_static/
    volumes:
      - static_volume:/frontend_static
  gateway:
    image: snake85/kitty_gateway
    env_file: .env
    volumes:
      - static_volume:/staticfiles/
      - media_volume:/home/kitty/media
    ports:
      - 8080:80
